# mkdocs local build output
site/

# restore-prep operator artifacts — site-specific schema/manifests, never commit to this public repo
/scrub/
/hooks/
*.scrub.json
*-stage-cleanup.sql

# Claude Code local state
.claude/settings.local.json
.claude/worktrees/

# boring (dev container) generated artifacts — regenerated from .boring/profile.yaml,
# contain absolute host paths; never commit
.devcontainer/
.boring/codegen/
